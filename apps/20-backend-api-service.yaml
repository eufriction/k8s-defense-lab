apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: backend
  labels:
    app: api
    app.kubernetes.io/part-of: demo
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
